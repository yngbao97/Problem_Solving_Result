SELECT COUNT(*) AS FISH_COUNT, MAX(b.LENGTH) AS MAX_LENGTH, a.FISH_TYPE
FROM FISH_INFO AS a
    JOIN (SELECT ID, IFNULL(LENGTH, 10) AS LENGTH
            FROM FISH_INFO) AS b
    ON a.ID = b.ID
GROUP BY a.FISH_TYPE
HAVING AVG(b.LENGTH) >= 33
ORDER BY a.FISH_TYPE