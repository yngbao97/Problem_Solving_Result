SELECT d.DEPT_ID AS DEPT_ID, d.DEPT_NAME_EN AS DEPT_NAME_EN, e.AVG_SAL AS AVG_SAL
FROM (SELECT DEPT_ID, ROUND(AVG(SAL), 0) AS AVG_SAL
      FROM HR_EMPLOYEES
      GROUP BY DEPT_ID) AS e
    JOIN HR_DEPARTMENT AS d
    ON e.DEPT_ID = d.DEPT_ID
ORDER BY AVG_SAL DESC